import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.infotangier.app',
  appName: 'info tangier',
  webDir: '/dist',
  bundledWebRuntime: false
};

export default config;
